</br>
<%= link_to 'Back', "/user/index" , class: "btn btn-default" %>
<p id="notice"><%= notice %></p>

<h1>People</h1>

<table>
  <thead>
    <tr>
      <th>Person</th>
      <th>Person first name</th>
      <th>Person last name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @people.each do |person| %>
      <tr>
        <td><%= person.person_id %></td>
        <td><%= person.person_first_name %></td>
        <td><%= person.person_last_name %></td>
        <td><%= link_to 'Edit', edit_person_path(person) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Person', '/people/userNew' %>
