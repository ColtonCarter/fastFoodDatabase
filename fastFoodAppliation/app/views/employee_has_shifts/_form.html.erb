<%= form_for(employee_has_shift) do |f| %>
  <% if employee_has_shift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee_has_shift.errors.count, "error") %> prohibited this employee_has_shift from being saved:</h2>

      <ul>
      <% employee_has_shift.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employee_id %>
    <%= f.number_field :employee_id %>
  </div>

  <div class="field">
    <%= f.label :shift_id %>
    <%= f.number_field :shift_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
