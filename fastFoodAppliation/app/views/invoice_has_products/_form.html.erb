<%= form_for(invoice_has_product) do |f| %>
  <% if invoice_has_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invoice_has_product.errors.count, "error") %> prohibited this invoice_has_product from being saved:</h2>

      <ul>
      <% invoice_has_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :order_id %>
    <%= f.number_field :order_id %>
  </div>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.number_field :product_id %>
  </div>

  <div class="field">
    <%= f.label :product_quantity %>
    <%= f.number_field :product_quantity %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
